export const UI = () => {
  return (
    <section className="fixed inset-0 z-10 flex items-center justify-center pointer-events-none">
      {/* Minimal UI container - can be extended as needed */}
    </section>
  );
};
